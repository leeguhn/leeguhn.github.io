# Guhn Lee — Personal Homepage

This directory contains the source for [leeguhn.github.io](https://leeguhn.github.io/).

Run `npm install` once, then use `npm run dev` for local development. `npm test`, `npm run lint`, and `npm run build` verify the site before deployment.
